/target/
.DS_Store
.env*
.idea/
.vscode/
*.swp
src-tauri/target/
src-tauri/gen/schemas/
app-tauri/gen/schemas/
node_modules/
# Local push helper — pulls GH token from `pass show SDL_GH`.
push.sh
# Built UI output (Vite). Regenerated by `npm run build` in ui/; tauri.conf.json
# `frontendDist` points at `../ui/dist`. Phase 4 made this a real build artifact;
# the previous phase-1 force-include of a hand-authored placeholder is removed.
dist/
ui/dist/
# Cargo.lock IS committed for this binary workspace (NOT a library) — see Cargo.toml
# header note. This pins transitive dep MSRVs (notify-types, indexmap, getrandom)
# to versions compatible with rustc 1.83; without it a clean clone resolves newer
# editions and the build breaks.
